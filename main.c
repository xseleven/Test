#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char **argv)
{
	int nData = 0;

	fprintf(stdout, "No = %d\n", nData);

	fprintf(stdout, "Hello World!\n");

	exit(EXIT_SUCCESS);
}
